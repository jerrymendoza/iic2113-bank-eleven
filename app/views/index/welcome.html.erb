<h1>Index#welcome</h1>
<p>Find me in app/views/index/welcome.html.erb</p>


<li><%= link_to current_user, '/users/sign_out', method: :delete, data: {confirm: 'Are you sure?'} %></li>
<li><%= link_to current_user, '/users', method: :delete, data: {confirm: 'Are you sure?'} %></li>
<li><%= link_to 'Registrarse',new_user_registration_path %></li>
<% if user_signed_in? %>
  <% current_user.accounts.each do |account|%>
    <td> <%=account.number.class%><td>
  <%end%>
<% end %>
