<p id="notice"><%= notice %></p>

<h1>Accounts of <%=@user.email%></h1>

<table>
  <thead>
    <tr>
      <th>Number</th>
      <th>Balance</th>
      <th>Account type</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @accounts.each do |account| %>
      <tr>
        <td><%= account.number %></td>
        <td><%= account.balance %></td>
        <td><%= account.account_type %></td>
        <td><%= account.user.email %></td>
        <td><%= link_to 'Show', [@user, account] %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

